# Bun configuration for stx project

# Note: Bun's fullstack dev server expects HTML files in routes.
# The stx plugin is used at build time to transform .stx -> .html
# See server.ts for how this works.
