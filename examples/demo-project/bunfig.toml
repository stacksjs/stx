# Bun configuration for stx project
# This enables importing .stx files directly in your code

[install]
# Install dependencies
peer = true

# Preload the stx plugin so .stx files can be imported at runtime
preload = ["bun-plugin-stx/preload"]
