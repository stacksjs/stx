<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>STX Component Demo</title>
  <script>
    module.exports = {
      title: "STX Component Demo",
      cards: [
        {
          title: "Getting Started",
          subtitle: "Your first steps with STX",
          content: "STX is a powerful templating engine for Bun. It allows you to write reusable components and build dynamic web applications.",
          variant: "primary"
        },
        {
          title: "Components",
          content: "Components in STX are reusable templates that can accept props and render dynamic content.",
          variant: "info"
        },
        {
          title: "Directives",
          content: "STX provides directives like @if, @foreach, and others to make your templates dynamic and powerful.",
          variant: "success",
          footer: "Learn more about directives in the documentation."
        }
      ],
      buttons: [
        { text: "Primary", variant: "primary" },
        { text: "Secondary", variant: "secondary" },
        { text: "Success", variant: "success", icon: "✓" },
        { text: "Danger", variant: "danger", icon: "✗" },
        { text: "Small", variant: "info", size: "sm" },
        { text: "Large", variant: "warning", size: "lg" },
        { text: "Disabled", variant: "primary", disabled: true }
      ]
    };
  </script>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      line-height: 1.6;
      max-width: 1000px;
      margin: 0 auto;
      padding: 2rem;
      color: #333;
    }

    .container {
      margin-bottom: 2rem;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
      margin: 2rem 0;
    }

    /* Card Styles */
    .card {
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
    }

    .card-header {
      padding: 1.5rem;
      border-bottom: 1px solid #eee;
    }

    .card-header h3 {
      margin: 0;
      font-size: 1.25rem;
    }

    .card-header h4 {
      margin: 0.5rem 0 0;
      font-size: 0.9rem;
      font-weight: normal;
      opacity: 0.7;
    }

    .card-body {
      padding: 1.5rem;
    }

    .card-footer {
      padding: 1rem 1.5rem;
      background: #f9f9f9;
      border-top: 1px solid #eee;
      font-size: 0.9rem;
      font-style: italic;
    }

    /* Card Variants */
    .card.primary {
      border-top: 4px solid #4299e1;
    }

    .card.success {
      border-top: 4px solid #48bb78;
    }

    .card.info {
      border-top: 4px solid #38b2ac;
    }

    .card.warning {
      border-top: 4px solid #ecc94b;
    }

    .card.danger {
      border-top: 4px solid #f56565;
    }

    /* Button Styles */
    .btn {
      padding: 0.5rem 1rem;
      border-radius: 4px;
      border: none;
      cursor: pointer;
      font-size: 1rem;
      display: inline-flex;
      align-items: center;
      margin: 0.5rem;
      transition: background-color 0.2s, transform 0.1s;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .btn:active {
      transform: translateY(0);
    }

    .btn-icon {
      margin-right: 0.5rem;
    }

    .btn-primary {
      background-color: #4299e1;
      color: white;
    }

    .btn-secondary {
      background-color: #718096;
      color: white;
    }

    .btn-success {
      background-color: #48bb78;
      color: white;
    }

    .btn-danger {
      background-color: #f56565;
      color: white;
    }

    .btn-warning {
      background-color: #ecc94b;
      color: #1a202c;
    }

    .btn-info {
      background-color: #38b2ac;
      color: white;
    }

    .btn-sm {
      padding: 0.25rem 0.75rem;
      font-size: 0.875rem;
    }

    .btn-lg {
      padding: 0.75rem 1.5rem;
      font-size: 1.125rem;
    }

    .btn[disabled] {
      opacity: 0.6;
      cursor: not-allowed;
      pointer-events: none;
    }

    /* Section Styles */
    .section {
      margin-bottom: 3rem;
    }

    .section-title {
      font-size: 1.75rem;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid #eee;
    }

    .buttons-container {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin: 2rem 0;
    }
  </style>
</head>
<body>
  <header>
    <h1>{{ title }}</h1>
    <p>This demo shows how to use components in STX</p>
  </header>

  <div class="section">
    <h2 class="section-title">Card Components</h2>

    <div class="grid">
      @foreach(cards as card)
        @include('components/Card', {
          title: card.title,
          subtitle: card.subtitle,
          content: card.content,
          footer: card.footer,
          classList: card.variant
        })
      @endforeach
    </div>
  </div>

  <div class="section">
    <h2 class="section-title">Button Components</h2>

    <div class="buttons-container">
      @foreach(buttons as button)
        @include('components/Button', {
          text: button.text,
          variant: button.variant,
          size: button.size,
          icon: button.icon,
          disabled: button.disabled
        })
      @endforeach
    </div>
  </div>

  <footer>
    <p>&copy; 2023 STX Components Demo</p>
  </footer>
</body>
</html>