<a
  href="{{ to }}"
  class="{{ className || '' }}"
  data-stx-link
  data-stx-active-class="{{ activeClass || 'active' }}"
  data-stx-exact-active-class="{{ exactActiveClass || 'exact-active' }}"
  @if(typeof prefetch !== 'undefined' && prefetch) data-stx-prefetch @endif
>{!! slot !!}</a>
