/**
 * @stacksjs/benchmarks
 *
 * Performance benchmarks comparing @stacksjs/stx and @stacksjs/sanitizer
 * against popular competitors in the ecosystem.
 *
 * Usage:
 *   bun run bench              - Run all benchmarks
 *   bun run bench:templating   - Run templating engine benchmarks
 *   bun run bench:sanitizer    - Run HTML sanitization benchmarks
 */

// Note: all-bench.ts is a CLI script, not meant to be imported
// Use: bun run bench:all
